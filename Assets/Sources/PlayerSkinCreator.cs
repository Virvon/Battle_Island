using UnityEngine;

public class PlayerSkinCreator : MonoBehaviour
{
    private void Start()
    {
        //var skin = Instantiate(Store.SelectItem.Skin.gameObject, transform.position, Quaternion.identity, transform);
    }
}
